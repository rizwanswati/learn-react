import React from "react";
import {Button} from "react-bootstrap";

function Bootsraping() {
    return(
        <div>
            <Button>Click Me</Button>
        </div>
    );
}

export default Bootsraping;